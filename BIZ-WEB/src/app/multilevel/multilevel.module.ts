import { NgModule } from '@angular/core';

import { MultilevelRoutingModule } from './multilevel-routing.module';

@NgModule({
  imports: [MultilevelRoutingModule],
})
export class MultilevelModule {}
